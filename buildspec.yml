version: 0.2

env:
  variables:
    NODE_ENV: "production"
    PORT: "3000"
phases:
  install:
    runtime-versions:
      nodejs: 22.8.0
    commands:
      - curl -fsSL https://rpm.nodesource.com/setup_22.x | bash -
      - yum install -y nodejs
      - npm install
      - node -v
      - npm -v
  build:
    commands:
      - npm run build
artifacts:
  files:
    - '**/*'